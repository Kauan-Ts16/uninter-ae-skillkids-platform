spring.application.name=backend

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

skillkids.jwt.secret=${JWT_SECRET}
skillkids.jwt.expiration=${JWT_EXPIRATION}

skillkids.admin.name=${ADMIN_NAME}
skillkids.admin.email=${ADMIN_EMAIL}
skillkids.admin.password=${ADMIN_PASSWORD}